# London Flip Finder -- pinned direct dependencies.
# Verified against Python 3.9.25; every pin below is 3.9-compatible.
#
#   python3 -m venv venv
#   ./venv/bin/pip install -U pip setuptools wheel
#   ./venv/bin/pip install -r requirements.txt
#   ./venv/bin/python -m ipykernel install --user --name london-flip-finder \
#       --display-name "London Flip Finder"

# --- core numerics ---------------------------------------------------------
numpy==1.26.4
pandas==2.3.3
scipy==1.13.1
pyarrow==21.0.0          # parquet cache for the master table

# --- geospatial ------------------------------------------------------------
geopandas==1.0.1
shapely==2.0.7
pyproj==3.6.1
pyogrio==0.11.1          # geopandas' default vector I/O engine

# --- modelling -------------------------------------------------------------
scikit-learn==1.6.1
xgboost==2.1.4
catboost==1.2.10
joblib==1.5.3

# --- visualisation ---------------------------------------------------------
matplotlib==3.9.4
seaborn==0.13.2

# --- notebook runtime ------------------------------------------------------
jupyterlab==4.5.10
ipykernel==6.31.0
nbformat==5.10.4
nbconvert==7.17.1        # enables the headless `--execute` end-to-end test

# --- utilities -------------------------------------------------------------
requests==2.32.5         # downloads the dataset release asset

# --- live-scan (optional, one-off: scripts/live_flip_scan/) ----------------
beautifulsoup4==4.15.0
lxml==5.3.0
